// The slide-over drawer was replaced by the full-page AccountView (fewer clicks, full transparency).
// Kept as a thin re-export so any lingering import of `money` still resolves.
export { money } from './AccountView'
