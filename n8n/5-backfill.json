{
  "name": "TN Brain · 5-BACKFILL · run once then delete",
  "nodes": [
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT graph_user_id, display_name FROM watchlist WHERE active = true;",
        "options": {}
      },
      "id": "w_list",
      "name": "Read watchlist",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        220,
        300
      ],
      "credentials": {
        "postgres": {
          "id": "tv0yKxg1fyTfyh8x",
          "name": "TN Azure DB"
        }
      }
    },
    {
      "parameters": {
        "method": "GET",
        "url": "=https://graph.microsoft.com/beta/users/{{ $json.graph_user_id }}/onlineMeetings/getAllTranscripts(meetingOrganizerUserId='{{ $json.graph_user_id }}',startDateTime={{ $json.start }},endDateTime={{ $json.end }})",
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "sendQuery": false,
        "options": {}
      },
      "id": "w_getall",
      "name": "Get transcripts (window)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        460,
        300
      ],
      "credentials": {
        "oAuth2Api": {
          "id": "QIB3e7FDXq9aL6tW",
          "name": "Microsoft Graph API"
        }
      },
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "// One watchlist member failing (404/permission blip) must not kill everyone's sweep:\n// keep only responses that actually contain a transcript array.\nconst ok=$input.all().filter(i=>Array.isArray(i.json&&i.json.value));\nconst dropped=$input.all().length-ok.length;\nif(dropped>0)console.log('watcher: skipped '+dropped+' failed Graph response(s)');\nreturn ok;"
      },
      "id": "w_valid",
      "name": "Keep valid responses",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        580,
        300
      ]
    },
    {
      "parameters": {
        "fieldToSplitOut": "value",
        "options": {}
      },
      "id": "w_split",
      "name": "Each transcript",
      "type": "n8n-nodes-base.splitOut",
      "typeVersion": 1,
      "position": [
        700,
        300
      ]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "=https://graph.microsoft.com/v1.0/users/{{ $('Read watchlist').item.json.graph_user_id }}/onlineMeetings/{{ $json.meetingId }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "options": {}
      },
      "id": "w_meet",
      "name": "Get meeting subject",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        940,
        300
      ],
      "credentials": {
        "oAuth2Api": {
          "id": "QIB3e7FDXq9aL6tW",
          "name": "Microsoft Graph API"
        }
      },
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "// Client-facing filter: keep a meeting only if at least one participant is outside TN.\nconst INTERNAL_DOMAINS=['tecknuovo.com']; // TN internal email domain(s) — confirm/extend if TN uses others\nconst p=$json.participants||{};\nconst ppl=[];if(p.organizer)ppl.push(p.organizer);(p.attendees||[]).forEach(a=>ppl.push(a));\nconst upns=ppl.map(a=>((a&&a.upn)||'').toLowerCase()).filter(Boolean);\nconst externals=upns.filter(u=>{const d=u.split('@')[1]||'';return d&&!INTERNAL_DOMAINS.includes(d);});\nconst external_domains=[...new Set(externals.map(u=>u.split('@')[1]))];\nconst attendee_check=upns.length?'ok':'unavailable';\n// Fail-open: if Graph gave no participant data, keep the call (the watchlist is the primary control) but flag it.\nconst is_external=attendee_check==='ok'?externals.length>0:true;\nreturn { json: { is_external, external_domains, attendee_check, subject: $json.subject||'' } };"
      },
      "id": "w_att",
      "name": "Attendee check",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1180,
        300
      ]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "=https://graph.microsoft.com/beta/users/{{ $('Read watchlist').item.json.graph_user_id }}/onlineMeetings/{{ $('Each transcript').item.json.meetingId }}/transcripts/{{ $('Each transcript').item.json.id }}/content",
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "$format",
              "value": "text/vtt"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "responseFormat": "text"
            }
          }
        }
      },
      "id": "w_content",
      "name": "Get transcript content",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1660,
        300
      ],
      "credentials": {
        "oAuth2Api": {
          "id": "QIB3e7FDXq9aL6tW",
          "name": "Microsoft Graph API"
        }
      },
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "function vttToText(v){\n  if(!v) return '';\n  const turns=[]; let cur=null; let inCue=false;\n  for(const raw of String(v).split(/\\r?\\n/)){\n    const l=raw.trim();\n    if(!l){ inCue=false; continue; }\n    if(/-->/.test(l)){ inCue=true; continue; }   // timestamp: cue payload follows\n    if(!inCue) continue;                          // WEBVTT header / cue id / NOTE -> skip\n    const m=l.match(/^<v\\s+([^>]+)>([\\s\\S]*)$/i);\n    if(m){\n      const speaker=m[1].trim();\n      const text=m[2].replace(/<\\/v>/ig,'').replace(/<[^>]+>/g,'').trim();\n      if(cur && cur.speaker===speaker){ cur.text += ' ' + text; }\n      else { cur={speaker,text}; turns.push(cur); }\n    } else {\n      const text=l.replace(/<[^>]+>/g,'').trim();\n      if(!text) continue;\n      if(cur){ cur.text += ' ' + text; } else { cur={speaker:'',text}; turns.push(cur); }\n    }\n  }\n  return turns.map(t => (t.speaker?t.speaker+': ':'')+t.text.replace(/\\s+/g,' ').trim()).filter(Boolean).join('\\n');\n}\n// Real call length: the largest cue END time in the VTT (we strip cue lines for\n// the text, but they carry the truth about duration). Handles h:mm:ss and mm:ss.\nfunction vttDuration(v){\n  let max=0;\n  for(const raw of String(v||'').split(/\\r?\\n/)){\n    const m=raw.match(/-->\\s*(?:(\\d{1,2}):)?(\\d{1,2}):(\\d{2})(?:[.,]\\d{1,3})?/);\n    if(m){ const s=(m[1]?+m[1]*3600:0)+(+m[2])*60+(+m[3]); if(s>max) max=s; }\n  }\n  return max;\n}\nconst e = s => String(s==null?'':s).replace(/'/g,\"''\");\nconst tid     = ($('Each transcript').item.json.id) || '';\nconst subject = ($('Get meeting subject').item.json.subject) || '';\nconst text    = vttToText($('Get transcript content').item.json.data || $('Get transcript content').item.json.body || '');\nconst durationSecs = vttDuration($('Get transcript content').item.json.data || $('Get transcript content').item.json.body || '');\nconst ac  = $('Attendee check').item.json;\nconst ext = JSON.stringify(ac.external_domains||[]);\n// Leadership-only meetings (Meesha, 22 Jul): these titles are visible ONLY on the\n// leadership dashboard. Extend this list when more names are agreed.\nconst LEADERSHIP_TITLES = ['business leadership', 'customer leadership', 'leadership team meeting'];\nconst visibility = LEADERSHIP_TITLES.some(t => (subject || '').toLowerCase().includes(t)) ? 'leadership' : 'all';\nconst sql =\n  \"INSERT INTO inbox (source, dedup_key, payload, status) \" +\n  \"SELECT 'teams', '\" + e(tid) + \"', jsonb_build_object(\" +\n    \"'account', COALESCE((SELECT name FROM accounts WHERE '\" + e(subject) + \"' ILIKE '%'||name||'%' ORDER BY length(name) DESC LIMIT 1), (SELECT name FROM client_directory WHERE '\" + e(subject) + \"' ILIKE '%'||name||'%' ORDER BY length(name) DESC LIMIT 1), 'UNKNOWN'),\" +\n    \"'title', '\" + e(subject) + \"',\" +\n    \"'visibility', '\" + visibility + \"',\" +\n    \"'duration_seconds', \" + (durationSecs||0) + \",\" +\n    \"'external_domains', '\" + e(ext) + \"'::jsonb,\" +\n    \"'attendee_check', '\" + e(ac.attendee_check||'') + \"',\" +\n    \"'dedup_key', '\" + e(tid) + \"',\" +\n    \"'transcript', '\" + e(text) + \"'\" +\n  \"), 'pending' \" +\n  \"WHERE '\" + e(text) + \"' <> '' \" +\n  \"ON CONFLICT (dedup_key) DO NOTHING RETURNING id;\";\nreturn { json: { sql, transcript_id: tid, subject, chars: text.length } };"
      },
      "id": "w_build",
      "name": "Build inbox insert",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1900,
        300
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.sql }}",
        "options": {}
      },
      "id": "w_insert",
      "name": "Insert to inbox",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        2140,
        300
      ],
      "credentials": {
        "postgres": {
          "id": "tv0yKxg1fyTfyh8x",
          "name": "TN Azure DB"
        }
      }
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "leftValue": "={{ $json.id }}",
              "rightValue": "",
              "operator": {
                "type": "string",
                "operation": "notEmpty",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "w_isnew",
      "name": "New call? (insert returned id)",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2380,
        300
      ]
    },
    {
      "parameters": {},
      "id": "bf_manual",
      "name": "Run backfill",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        -900,
        0
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// One item per (person x week) from 22 Jul to now. Small windows avoid any\n// Graph date-range limits, and a failure in one chunk cannot hide the rest.\nconst START = Date.parse('2026-07-22T00:00:00Z');\nconst WEEK = 7 * 24 * 3600 * 1000;\nconst now = Date.now();\nconst out = [];\nfor (const item of $input.all()) {\n  const u = item.json;\n  for (let t = START; t < now; t += WEEK) {\n    out.push({ json: { ...u,\n      start: new Date(t).toISOString().replace(/\\.\\d{3}Z$/, 'Z'),\n      end: new Date(Math.min(t + WEEK, now)).toISOString().replace(/\\.\\d{3}Z$/, 'Z'),\n    } });\n  }\n}\nreturn out;"
      },
      "id": "bf_chunks",
      "name": "Week chunks per person",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -500,
        0
      ]
    }
  ],
  "connections": {
    "Read watchlist": {
      "main": [
        [
          {
            "node": "Week chunks per person",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get transcripts (window)": {
      "main": [
        [
          {
            "node": "Keep valid responses",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Each transcript": {
      "main": [
        [
          {
            "node": "Get meeting subject",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get meeting subject": {
      "main": [
        [
          {
            "node": "Attendee check",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get transcript content": {
      "main": [
        [
          {
            "node": "Build inbox insert",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build inbox insert": {
      "main": [
        [
          {
            "node": "Insert to inbox",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Insert to inbox": {
      "main": [
        [
          {
            "node": "New call? (insert returned id)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "New call? (insert returned id)": {
      "main": [
        []
      ]
    },
    "Attendee check": {
      "main": [
        [
          {
            "node": "Get transcript content",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Keep valid responses": {
      "main": [
        [
          {
            "node": "Each transcript",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run backfill": {
      "main": [
        [
          {
            "node": "Read watchlist",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Week chunks per person": {
      "main": [
        [
          {
            "node": "Get transcripts (window)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}